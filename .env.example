OPENSHELF_PASSWORD=changeme

# Optional: Bun server port. Docker installs should leave this as 3000.
# PORT=3000

# Optional: host port used by Docker Compose.
# OPENSHELF_HOST_PORT=3000

# Optional: enable only when OpenShelf is served in its production env and over HTTPS.
# NODE_ENV=production
